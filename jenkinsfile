pipeline {
    agent any
stages{
		 stage('Build'){
            steps {
            bat 'cmd /c mvn clean package'
            }
            post{
                success{
                    echo 'Now Archiving'
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }  
    }
}