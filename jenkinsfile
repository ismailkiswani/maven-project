pipeline {
    agent any
stages{
		 stage('Build'){
            steps {
            bat "${cmd.exe /k mvn clean package}"
            }
            post{
                success{
                    echo 'Now Archiving'
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }  
    }
}