pipeline {
    agent any
stages{
		 stage('Build'){
            steps {
                def package='package'
            bat "mvn clean ${package}"
            }
            post{
                success{
                    echo 'Now Archiving'
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }  
    }
}