pipeline {
    agent any
stages{
		 stage('Build'){
            steps {
              cmd.exe /c mvn clean package
            }
            post{
                success{
                    echo 'Now Archiving'
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }  
    }
}